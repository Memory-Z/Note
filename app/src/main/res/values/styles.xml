<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlueGray</item>
        <item name="colorPrimaryDark">@color/colorLightBlueGray</item>
        <item name="colorAccent">@color/colorBlueGray</item>
    </style>
    
    <style name="NavigationBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@color/colorBlueGray</item>
    </style>

</resources>
